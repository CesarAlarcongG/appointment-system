/*Por el momento no se que debo recibir en el endpoint, por lo que lo dejo comentado :v

import { User } from '../../entities/user.entity';
import { UserRegister } from '../register.interface';




export class GoogleRegisterService implements UserRegister<> {
  registerInformation(information: ): Promise<User> {}
}
*/
